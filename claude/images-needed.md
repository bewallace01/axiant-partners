# Images needed

Articles ship without a hero `<figure>` rather than with a broken `<img>`. Each
entry below is a slot waiting on an asset. When one lands in `assets/`, add the
`<figure class="article-figure">` block to the article (see
`sba-loans/articles/how-long-sba-loan-approval/` for the picture/source/img
shape and the 800w/1200w srcset convention).

## DSCR cluster — batch 1 (2026-09-02)

| Article | Slot | What the image should show |
|---|---|---|
| `dscr-loans/articles/how-dscr-is-calculated/` | hero | The ratio as arithmetic — a rent figure over a payment figure. Avoid stock handshakes. |
| `dscr-loans/articles/what-counts-as-rental-income-dscr/` | hero | A lease and an appraisal rent schedule side by side; the article is about which one governs. |
| `dscr-loans/articles/dscr-loans-short-term-rentals/` | hero | A furnished short-let interior, or a calendar of nightly bookings. Must read as short-term, not a long-term rental. |

Nothing DSCR-specific exists in `assets/` today — the nearest are the `cre-*`
files, which are commercial-real-estate framing and would misrepresent a
single-family rental article.
